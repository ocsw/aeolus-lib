git (gitolite?  github?)
versioning
checker script
pull out todos
README.md

pull out wrapper?

percona backup?

integrated pg_dumpall -g

notes on rdiff-backup modes, restores, 1-arg commands (incl nodefault list)

rdiff - list incr?

no dbs, empty dbnames, etc.

on_err: status values (reasons for phase/etc)
