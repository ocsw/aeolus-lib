#
# egrep wrapper: egrep vs. grep -E
#
# global vars: egreptype
#
do_egrep () {
  if [ "$egreptype" = "egrep" ]; then
    egrep ${1+"$@"}
  else
    grep -E ${1+"$@"}
  fi
}



checklist "egreptype" "$egreptype" "egrep" "grepE"



# which does the system have, egrep ("egrep") or grep -E ("grepE")?
# if both, either can be used
# if neither... that's a problem
egreptype="egrep"



egrep is only needed if grep doesn't have the -E option (see egreptype)
