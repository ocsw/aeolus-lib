git (gitolite?  github?)
checker script

rdiff
integrated pg_dumpall -g

script, docs re library install and source
eval funcs - unset vs null, esp. arrays (w/ declare -p)
set -x for commands?  but remember retvals



sbm threshold: null?

exitvals
replstatus filename and status
blank non-existent?  remove all, let error?
