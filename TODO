git (gitolite?  github?)
checker script

postgres
rsync log (incl. rotation)
rdiff

script, docs re library install and source
eval funcs - unset vs null, esp. arrays (w/ declare -p)
set -x for commands?  but remember retvals

test rot/pru/pr with all 5
test ex/rm/mv fz with bz, xz, all->both

sbm threshold

psql -Atc 'select datname from pg_catalog.pg_database;'

       -c, --clean
       -a, --data-only
       -s, --schema-only
       --column-inserts
       --inserts
       --lock-wait-timeout=timeout
       --quote-all-identifiers
       -x, --no-privileges, --no-acl
       -O, --no-owner
       --no-tablespaces

       -C, --create

       -g, --globals-only
       -r, --roles-only
       -t, --tablespaces-only

restore: -c -C -a -s -x
